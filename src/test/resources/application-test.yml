spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
      dialect: PostgreSQLDialect
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  jackson:
    serialization:
      write-dates-as-timestamps: false

logging:
  level:
    root: WARN
    com.daniil.booksonline: DEBUG

